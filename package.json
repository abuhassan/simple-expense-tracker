{
  "name": "simple-expense-tracker",
  "version": "1.0.0",
  "description": "Backend for simple expense tracker",
  "main": "server.js",
  "scripts": {
    "frontend": "cd client && npm start",
    "server": "node --watch server",
    "backend": "cd backend && npm start",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\""
  },
  "author": "Abu hassan",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
